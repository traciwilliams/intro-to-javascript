window.onload = function(even) {
    //widow object's ability to do things onload
    // use window.onload - to make sure your content is all loaded
    console.log("The page has loaded!"); //the console will output something
    console.log("Thanks, window.onload");
};